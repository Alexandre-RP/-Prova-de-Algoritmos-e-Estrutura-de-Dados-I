# Solucao Forense (AED) - Repositorio Gerado

Estrutura Maven pronta para compilação. **Substitua** `lib/analise-forense-api.jar` pelo JAR da disciplina (fornecido pelo professor).

## Como compilar
```bash
mvn clean package
```
O FAT JAR será gerado em `target/`. Renomeie conforme pedido pelo avaliador.

## Observações
- `README.txt` contém apenas o nome da classe que implementa a interface (exigência do enunciado).
- O arquivo PDF original do enunciado foi copiado para `docs/Trabalho 2 - AED.pdf`.
